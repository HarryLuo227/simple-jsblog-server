{
  "name": "simple-jsblog-server",
  "version": "0.0.0",
  "description": "A simple practice project of blog server developed in JavaScript.",
  "main": "app.js",
  "scripts": {
    "dev": "node --env-file=./envs/localhost.env app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Harry",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.3",
    "morgan": "^1.10.0",
    "pg": "^8.11.3"
  }
}
